using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class player : MonoBehaviour
{
    // Start is called before the first frame update
    public string text_1 = "这是一条来自啦啦啦啦的修改";
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
